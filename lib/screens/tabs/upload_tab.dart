import 'package:flutter/material.dart';

class UploadTab extends StatelessWidget {
  const UploadTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Upload New Photo', style: TextStyle(color: Colors.white)),
    );
  }
}
